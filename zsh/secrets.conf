# Maps environment variables to 1Password secret references.
# Read by the load-secrets function (zsh/zshrc.symlink).
#
# Format (no spaces around the =):
#   ENV_VAR=op://<vault>/<item>/<field>
OPENAI_API_KEY=op://Private/OpenAI API Key/credential
